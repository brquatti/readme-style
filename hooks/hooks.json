{
  "description": "readme-style: advisory SessionStart check. Prints a note when README.md is missing or off-layout. Never writes, never blocks.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-readme.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
